"use client";
import { Card, CardContent } from "../ui/card";
import { WebContainerType } from "@/app/types/user";

export default function fileSideBar(files : WebContainerType) {
  
  return (
    <Card>
        {}
    </Card>
  );
}
